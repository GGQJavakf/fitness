# 0.1.2 技术发布候选验证记录

当前复核日期：2026-09-01（Asia/Shanghai）

## 当前结论

`0.1.2` 当前工作区**尚不是可放行的冻结候选**。2026-08-20 的一次性 MySQL 和打包运行证据只适用于当时的候选快照；当前 `HEAD` 为 `cf31f86f028f5b9cfb3766c90634813524979dfe`，且工作区存在 300 余项后续修改、删除和未跟踪文件，旧证据不能替代当前候选的重新验证。

2026-09-01 当前复核结果：

- 小程序生产依赖和完整工具链审计均为 0 个已知漏洞，OpenAPI 类型、专业审核包一致性、TypeScript 类型检查通过；93 个测试文件、761 项测试通过，微信构建、预览入口、异步分包边界和包体积门禁通过。
- `staging-experience` 发布配置预检通过；当前构建已通过微信开发者工具 `auto_preview`，总包 5,080,380 字节、主包 377,949 字节。该结果证明预览包可生成并推送，不代表手机端真人旅程或体验环境部署已经完成。
- 后端已在 WSL Linux Java 21.0.12 和本机隔离 MySQL 8.0.46 上完成 `clean verify`：Surefire 500 项、Failsafe 2 项，失败 0、错误 0、跳过 0；覆盖率门禁通过。真实 MySQL 迁移、并发检查和最终打包 JAR 启动健康检查均已实际执行。
- 本轮真实 MySQL 首次复核发现发布门禁仍把迁移上限写成 V024，而仓库已存在 V025。门禁已同步到 V025，并增加“门禁版本列表必须与 classpath 迁移文件完全一致”的回归测试；重新验证后 25 个迁移全部通过。
- Maven 运行时依赖审计覆盖 49 个坐标，未发现 OSV 已知漏洞匹配。
- 正式后端目标已由仓库 `Dockerfile` 的 `eclipse-temurin:21-jre-jammy` 基础镜像、CloudBase Run 容器接入方式和 Ubuntu CI 共同确认为 Linux。当前 Windows JDK 的 AF_UNIX/Selector 回环连接错误只影响本机 Windows 启动；同一打包 JAR 已在目标同类 Linux Java 21 环境通过，因此该主机问题不再列为首发阻塞。
- `public` 预检仍按预期阻断 7 个汇总项：3 类内容尚未从 `AI_VALIDATED` 晋级并启用公开环境，78 条动作替代关系尚无专业签署。用户对健身计划逻辑的确认不自动构成专业内容签署。
- 当前工作区尚未部署到体验环境，未获得当前候选的 Android/iPhone 真机证据；体验环境数据库密码和微信 AppSecret 在公开发布前仍需完成轮换并回读验证。

因此，当前代码层、真实数据库和 Linux 打包运行的本地技术阻塞已清除；但工作区仍未冻结成唯一提交，体验环境、真机和专业内容审核也未完成，仍不得据此执行正式上传、提审或公开发布。

## 2026-09-01 候选范围审计与冻结清单

当前分支为 `codex/m0-baseline`，`HEAD` 与上游均为 `cf31f86f028f5b9cfb3766c90634813524979dfe`。按 `git status --porcelain=v1 -uall` 逐文件统计，工作区共有 399 个修改、删除或未跟踪路径；其中 396 个属于拟纳入 `0.1.2` 的产品、测试、规格和发布记录变更，以下 3 个仅为本机运行痕迹，必须排除：

- `.day-option`
- `.playbook/state/doctor-cache/2026-08-30.json`
- `.playbook/state/doctor-cache/2026-08-31.json`

本地环境文件 `.env.staging-experience.local`、微信开发者工具配置 `miniprogram/project.config.json`、前端构建目录 `miniprogram/dist` 和后端构建目录 `backend/target` 均受 Git 忽略规则保护，不属于候选提交。候选清单中未发现这些本地环境或构建路径被误纳入。

候选范围按模块统计为：小程序 317 个、后端 59 个、规格 8 个、规则配置 6 个、契约 3 个、发布文档 3 个；按状态统计为修改 290 个、新增 102 个、删除 4 个。本轮隔离 MySQL 验证下载的 2 个 `.deb` 临时安装包已识别并移出仓库，不属于候选。

冻结前敏感信息与产物审计结果：396 个候选路径中有 4 个删除项，其余 392 个现存文件未发现私钥、证书库、`.env` 或其他敏感文件名；启发式内容扫描产生的 75 条线索全部位于测试夹具，且均未匹配本机实际配置值。本机数据库密码和微信 AppSecret 与候选源码、小程序构建目录及后端 JAR 的精确值比对均无命中；非秘密的微信 AppID 仅按预期存在于被忽略的 `miniprogram/dist/project.config.json`。后端 JAR 共 931 个条目，未包含测试类、源码、环境文件、密钥文件或 source map；小程序构建目录共 279 个文件，未发现上述敏感文件名或 source map。

4 个删除项均已确认是目录拆分后的迁移，而不是功能误删：

- `miniprogram/src/platform/weapp/compositionRoot.ts` 已拆分为共享平台内核、功能核心和 6 个功能组合根；应用页面已改用对应功能根。
- `miniprogram/src/presentation/pages/home/loginSingleFlight.ts` 已迁移到启动分包，登录单飞行为保持不变。
- exercise-guide 分包中的 workout-session 页面及配置已迁移到 workout 分包；页面改为通过异步功能加载器加载训练会话代码，应用路由同步指向新位置。

首发规格复核结果：

- Android 启动隔离 T1-T7 已完成；T8 的 Android 真机首页、计划、训练、历史和账户旅程仍需在冻结候选上执行。
- 预设后优化的首发核心实现、测试、构建和本地审核已完成。器械清单 V2、通用保守单侧时长模型和弹力带安全锚点已明确延期到 `0.2`，不阻塞 `0.1.2`；弹力带预设继续保持 `BLOCKED_CAPABILITY`，不得伪装成已支持。
- 当前工作区的独立 MySQL 迁移、并发验证和 Linux 打包运行已完成；仍未完成的是候选冻结、体验环境真人旅程、Android/iPhone 真机矩阵，以及公开内容的合格专业人员签署。

上述 396 个路径目前只是**拟冻结范围**，尚未暂存、提交或部署。冻结顺序应为：排除 3 个本机文件并按清单暂存 → 复核暂存差异 → 经授权提交形成唯一候选版本 → 对冻结版本复跑门禁并记录制品摘要 → 部署到体验环境 → 完成真人旅程和真机验收。凭据轮换、候选提交、体验环境部署、微信上传/提审和公开发布分别需要对应授权；任何一步均不得从本记录自动推断为已获授权。

## 2026-09-01 当前工作区真实 MySQL 与打包证据

- 验证环境：WSL Ubuntu 24.04、OpenJDK 21.0.12、Maven 3.9.13、本机回环隔离 MySQL 8.0.46；数据库不监听外部网卡，验证实例不使用生产或体验数据。
- 完整命令结果：`clean verify` 成功，Surefire 108 份报告共 500 项，Failsafe 1 份报告共 2 项，合计 502 项，失败 0、错误 0、跳过 0；JaCoCo 对 622 个类的覆盖率门禁全部满足。
- 数据库结果：Flyway V001-V025 在空库实际执行并校验；发布标记绑定同一 JDBC 目标、数据库名、MySQL `server_uuid` 和 25 个 checksum，打包 smoke 消费后删除。
- 打包运行结果：`fitness-assistant-backend-0.1.2.jar` 在 Linux Java 21 上启动成功，健康状态和数据库状态均为 UP，受保护业务路由可访问且匿名访问保持拒绝。
- 当前工作区制品 SHA-256：`87CC3DB995C2FE3B01AE96456904B0F8189EEE600FD3ED4255AFDEC05CF9A821`。该摘要只对应尚未提交的当前工作区构建；冻结提交后必须重新生成并记录正式候选摘要。

历史验证日期：2026-08-20（Asia/Shanghai）

## 2026-08-20 历史结论

`0.1.2` 已达到可复现的技术发布候选状态，但**尚不具备公开发布资格**。自动化门禁、关键页面模拟器检查和一次性空 MySQL 8 打包验证均已通过；公开内容专业审核和微信真机弱网恢复矩阵仍是外部门禁。该结论不代表生产部署、微信平台提审或公开发布完成。

## 2026-08-20 历史证据

| 范围 | 结果 | 证据 |
| --- | --- | --- |
| 小程序依赖 | 通过 | 生产依赖和完整工具链 `npm audit --audit-level=low` 均为 0 个已知漏洞；`vm2` 实际解析为 `3.11.6`。 |
| API 与生成物 | 通过 | OpenAPI 类型无漂移；专业审核包重新生成检查无漂移。 |
| 小程序质量门 | 通过 | TypeScript 类型检查通过；68 个测试文件、472 项测试通过；微信小程序构建及主包/分包大小门禁通过。 |
| 可用性 | 通过（模拟器） | 共享字号/点击区回归测试通过；计划页、渐进式计划编辑器和训练页在微信开发者工具紧凑视口完成目视检查，控制台未发现运行时错误。 |
| 后端单元/集成测试 | 通过 | Surefire 99 个报告、438 项测试，失败 0、错误 0、跳过 0。 |
| 打包 MySQL smoke | 通过 | Failsafe `PackagedApplicationSmokeIT` 1 项通过，失败 0、错误 0、跳过 0；覆盖 Flyway、DataSource、健康状态、匿名拒绝、鉴权业务路由和共享 consumer sample。 |
| 一次性数据库边界 | 通过 | 使用获批的非生产端点创建唯一空 schema `fitness_verify_20260820rc012c`；验证前确认为空，连接为加密链路；验证后按所有权标记精确删除，未修改既有 `fitness_m0`。 |
| 后端依赖漏洞 | 通过 | 对 49 个 Maven 运行时坐标执行 OSV 查询，未发现已知漏洞匹配。 |
| 体验预览 | 部分通过 | 预览构建成功并推送到已登录的微信开发者工具；未执行上传、提审、公开发布或生产部署。 |

## 安全与环境说明

外部 MySQL 端点的证书链无法在当前 JDK 信任库中完成身份校验。本次验证依据项目既有、明确记录的风险接受，仅对获批的一次性空验证 schema 使用加密但不校验证书身份的连接；因此该结果证明 MySQL 8 兼容性、迁移和打包运行，不证明端点身份或抗中间人能力。生产或长期环境必须使用可信 CA 的 `VERIFY_CA`/`VERIFY_IDENTITY`。

## 保持失败关闭的门禁

### 公开内容审核

`npm run preflight:release` 当前按预期阻断 7 类汇总项：

- 3 份内容仍为 `AI_VALIDATED`，不是健身专业人员批准；
- 3 份内容未激活 `public` 发布环境；
- 78 条动作替代关系尚未获得专业批准。

审核材料已生成至 `docs/public-content-professional-review-pack.md`，审核人、资质、日期、逐项结论和签署引用均保持空白。自动化、AI 或开发者检查不得代填这些字段。

### 微信真机弱网恢复

自动预览已生成，但手机端自动化在等待设备连接时超时，尚未获得物理设备证据。以下场景仍需按 `docs/device-debug-runbook.md` 在真实微信中执行并记录：

- 登录和计划加载超时/重试；
- 训练草稿恢复与最新持久状态保留；
- 记录组、保存和响应丢失后的幂等恢复；
- 弱网、断网、飞行模式、锁屏、杀进程和连续点击。

在设备型号、系统/微信/基础库版本、候选版本、时间、预期和实测结果完整记录前，不得宣称真机门通过。

## 可复现命令

```powershell
Set-Location miniprogram
npm ci
npm run verify
npm run preflight:staging
npm run preflight:release # 公开审核未完成时预期退出码为 2

Set-Location ..\backend
.\mvnw.cmd clean verify

Set-Location ..
git diff --check
git status --short
```

外部 MySQL 验证仅可使用明确授权的非生产一次性空 schema，并通过本地密钥注入；不得把连接串、用户名或密码写入仓库或验证日志。

## 回滚

候选尚未推送或发布时，可按提交边界回退；计划编辑器改动只涉及表现层状态、样式和回归测试，不修改已存计划语义。任何公开审核元数据更新必须由专业审核证据驱动并单独复验。
